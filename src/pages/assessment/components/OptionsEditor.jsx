import React from "react";

export default function OptionsEditor({ options, onChange }) {
  return (
    <div>
      <h4 className="font-medium">Options Editor</h4>
      {/* Add/remove choice options here */}
    </div>
  );
}
